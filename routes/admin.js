const express=require('express');const auth=require('../controllers/adminAuthController');const products=require('../controllers/adminProductController');const categories=require('../controllers/adminCategoryController');const orders=require('../controllers/adminOrderController');const dashboard=require('../controllers/adminDashboardController');const {requireAdmin}=require('../middleware/auth');const {requireCsrf}=require('../middleware/csrf');const {upload}=require('../services/imageService');const router=express.Router();
router.get('/',auth.root);router.get('/setup',auth.setupPage);router.post('/setup',auth.setup);router.get('/login',auth.loginPage);router.post('/login',auth.login);router.post('/logout',requireAdmin,auth.logout);router.use(requireAdmin);
router.get('/dashboard',dashboard.page);router.get('/products',products.list);router.get('/products/new',products.newPage);router.get('/products/:id/edit',products.editPage);router.post('/products',upload.array('images',4),requireCsrf,products.create);router.post('/products/:id',upload.array('images',4),requireCsrf,products.update);router.post('/products/:id/delete',products.remove);
router.get('/categories',categories.page);router.post('/categories',categories.create);router.post('/categories/:id',categories.update);router.post('/categories/:id/delete',categories.remove);
router.get('/orders',orders.list);router.get('/orders/:id',orders.detail);router.post('/orders/:id/status',orders.updateStatus);module.exports=router;

const express=require('express');const rateLimit=require('express-rate-limit');const {create}=require('../controllers/orderController');const {search}=require('../controllers/storefrontController');const router=express.Router();router.get('/search',search);router.post('/orders',rateLimit({windowMs:10*60*1000,max:20,standardHeaders:true,legacyHeaders:false}),create);module.exports=router;
